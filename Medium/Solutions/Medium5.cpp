#include<iostream>
#include<queue>

using namespace std;

int main(){
    int w, n;
    cin >> w >> n;
    queue<int> arr;
    int a;
    cin >> a;
    arr.push(a);
    int wait[];
    for (int i = 1; i < w; i++){
        cin >> a;
        if (a > queue.back())
    }
}
